---
title: The API
description: One envelope, twenty-five payload kinds, a local HTTP surface, and two SDKs generated from a single contract artefact.
sidebar:
  order: 0
---

import { CardGrid, LinkCard } from "@astrojs/starlight/components";

Everything lemonfiber can tell you, it can tell a machine. Every answer arrives
in one wrapper — `api_version`, `kind`, `data` — and that wrapper is a stable,
versioned interface rather than output that happens to be parsable.

It reaches you two ways. [`--json`](/commands/global-flags/) on the command line
prints it. `lemonfiber ui` serves it over local HTTP: six read endpoints,
`POST /api/actions/<name>` for anything that changes something, and
`GET /api/events` as a live stream — all of it behind a token minted for that run,
on a socket bound to this machine. Both surfaces dispatch through the same entry
point, so neither can say something about your stack that the other would not.

What the binary does **not** yet carry is the web app itself, so `lemonfiber ui`
today serves the API and no interface unless `--assets` points it at a build. The
shape of the surface is [the web API contract](/spec/20-architecture/contracts/web-api/);
[the envelope](/api/the-envelope/) covers the endpoints, the guard, and what is
still missing.

The two SDKs below are written against that contract and generate their types from
the same artefact the command line serialises with, so what they parse is what both
surfaces already emit.

<CardGrid>
  <LinkCard
    title="The envelope"
    href="/api/the-envelope/"
    description="The three fields every payload arrives in, and the rules a client has to keep."
  />
  <LinkCard
    title="Every payload kind"
    href="/api/kinds/"
    description="Every kind the contract describes, and the six documented field by field."
  />
  <LinkCard
    title="The TypeScript SDK"
    href="/api/typescript-sdk/"
    description="Typed calls, a typed event stream and a typed error, with no runtime dependencies."
  />
  <LinkCard
    title="The PHP SDK"
    href="/api/php-sdk/"
    description="A peer of the TypeScript client, built on Saloon, with one class per kind."
  />
  <LinkCard
    title="Two version numbers"
    href="/api/two-version-numbers/"
    description="Why the package version and the wire version are different numbers doing different jobs."
  />
</CardGrid>

The shapes are generated and the meanings are not. Fields, types, optionality and
permitted enum values come from a contract artefact built out of the Rust types
that actually serialise the reply, so no client transcribes them by hand.
Everything a schema cannot express — the heartbeat, what a resumed stream may
claim is current, where the token travels — is prose in the contract, and every
client implements and tests it.

## Where to go next

If you are writing a script rather than a client, [the global flags](/commands/global-flags/)
covers `--json` and [the commands](/commands/) covers what you can ask for. If
you are building against the boundary itself,
[the architecture notes](/develop/architecture/) explain how the core is put
together behind it.
